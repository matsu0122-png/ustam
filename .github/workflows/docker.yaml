name: docker

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "Image tag to publish (e.g. 0.1.2). Defaults to the ref name with a leading v stripped."
        required: false
        default: ""

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image metadata
        id: vars
        env:
          INPUT_VERSION: ${{ github.event.inputs.version }}
        run: |
          # リリースタグ（v付き）からバージョン番号を取り出す。
          # 手動実行時に version を指定した場合はそちらを優先する。
          REF_NAME="${GITHUB_REF_NAME}"
          VERSION="${INPUT_VERSION:-${REF_NAME#v}}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "revision=${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
          echo "created=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Containerfile
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            VERSION=${{ steps.vars.outputs.version }}
            GIT_REVISION=${{ steps.vars.outputs.revision }}
            BUILD_DATE=${{ steps.vars.outputs.created }}
          tags: |
            ghcr.io/${{ github.repository }}:${{ steps.vars.outputs.version }}
            ghcr.io/${{ github.repository }}:latest
